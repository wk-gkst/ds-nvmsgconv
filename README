## Custom implementation for msg2p-lib

refer to https://docs.nvidia.com/metropolis/deepstream/dev-guide/text/DS_plugin_gst-nvmsgconv.html

## Pre-requisites:
- glib-2.0
- json-glib-1.0
- uuid
- yaml-cpp
- deepstream 6.1

Install using:
  sudo apt-get install libglib2.0-dev libjson-glib-dev uuid-dev libyaml-cpp-dev

## Compiling and installing the plugin:
Run `make` and `sudo make install`

NOTE: To compile the sources, run make with "sudo" or root permission.
